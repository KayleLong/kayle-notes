#!/usr/bin/env python
#-*- coding: utf-8 -*
#@Code:Kayle Long
#@Date 2017.02.06

def max(a, b):
	if a > b:
		print 'a=%d, b=%d,\na is max number.' %(a, b) #\n 是换行
	else:
		print 'a=%d, b=%d,\nb is max number.' %(a, b)

max(7, 5)
x = 100
y = 200
max(x, y)
